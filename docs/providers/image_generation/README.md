# Image-generation provider documentation

Place the selected provider's official image generation/editing API
documentation and sanitized examples here. The matching adapter must implement
the common `ImageProvider` interface under `app/providers/`.

Do not commit API keys, bearer tokens, or private generated assets.
